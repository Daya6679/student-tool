# Playwright Always Pass Project

This is a ready-to-run Playwright project where all tests **always pass**.  

## Setup

1. Clone the repo:

```bash
git clone <your-repo-url>
cd playwright-always-pass
# student-tool
# test webhook
